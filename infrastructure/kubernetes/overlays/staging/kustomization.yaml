apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# staging overlay — prod-identical topology; SLO/load/chaos/DR proving ground (docs/10 §1).
resources:
  - ../../base
  - argocd-application.yaml

labels:
  - pairs:
      nexus.io/env: staging
    includeSelectors: false
