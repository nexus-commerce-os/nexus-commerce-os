apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# dev overlay — shared integration cluster, trunk-latest (docs/10 §1).
resources:
  - ../../base
  - argocd-application.yaml

labels:
  - pairs:
      nexus.io/env: dev
    includeSelectors: false
