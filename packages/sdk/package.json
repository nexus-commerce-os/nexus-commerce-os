{
  "name": "@nexus/sdk",
  "version": "0.0.0",
  "private": true,
  "description": "NEXUS typed client SDK (generated from the single contract source) — P0.1 scaffold (empty).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@nexus/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
