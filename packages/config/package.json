{
  "name": "@nexus/config",
  "version": "0.0.0",
  "private": true,
  "description": "NEXUS shared TS config + (future) canonical money type — P0.1 scaffold; money type is a P0.4 deliverable.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
