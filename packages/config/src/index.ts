// @nexus/config — shared TS config + canonical money type (FUTURE).
// P0.1 SCAFFOLD — empty export only.
//
// P0.4 will add the canonical money type here: integer minor-units + ISO currency,
// defined once and code-generated into TS/Go/Python (ADR-0020, NFR-CONS-01).
// Do NOT implement money arithmetic in the scaffold.
export {};
