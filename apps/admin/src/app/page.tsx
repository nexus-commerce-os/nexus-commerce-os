// P0.1 scaffold placeholder — NOT a real page.
// No routes, auth, RBAC, dashboards, or admin logic.
// The real operator console is built from P0.2 onward (see ./README.md).
export default function Placeholder() {
  return <main>NEXUS admin — scaffold (P0.2+)</main>;
}
