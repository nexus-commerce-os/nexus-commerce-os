// P0.1 scaffold placeholder — NOT a real page.
// No routes, data fetching, search, agent, or commerce logic.
// The real shopper surface is built from P0.2 onward (see ./README.md).
export default function Placeholder() {
  return <main>NEXUS web — scaffold (P0.2+)</main>;
}
