{
  "name": "@nexus/catalog",
  "version": "0.0.0",
  "private": true,
  "description": "NEXUS Catalog & Offer module (NestJS/TS) — P0.1 scaffold. No catalog logic; implemented in P0.3.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"no tests — scaffold\" && exit 0"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.28",
    "@nestjs/core": "^10.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
