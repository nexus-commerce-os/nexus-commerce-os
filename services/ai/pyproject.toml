# NEXUS AI Serving + Agent — Python project manifest (P0.1 scaffold).
# Declares the module only. No runtime dependencies, no logic. Implemented in P0.5.
[project]
name = "nexus-ai"
version = "0.0.0"
description = "NEXUS AI Serving + Agent (FastAPI) — P0.1 scaffold. No AI logic; implemented in P0.5."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nexus_ai"]
