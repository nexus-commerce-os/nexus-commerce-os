{
  "name": "@nexus/auth",
  "version": "0.0.0",
  "private": true,
  "description": "NEXUS Identity & Profile module (NestJS/TS) — P0.1 scaffold. No auth logic; implemented in P0.2.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"no tests — scaffold\" && exit 0"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
